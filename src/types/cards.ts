export type SmallCard = {
  id: number;
  name: string;
  vendorCode: string;
  type: string,
  previewImg: string,
  stringCount: number,
  rating: number,
  description: string,
  price: number,
};

export type SmallCards = SmallCard[];
